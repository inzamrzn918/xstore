# NPM Configuration for XStore

# Disable package-lock.json in root (we use it in subprojects)
package-lock=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Set log level
loglevel=warn
